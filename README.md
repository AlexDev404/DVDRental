# DVDRental

A DVD Rental system.
You can build using either MingW32, Linux, or Visual Studio (2019).

On Linux, you would open up the terminal and run

```shell
make clean && make

# And then run using

./main.exe
```

On Visual Studio (2019), you can just open up the solution and then build and run.
